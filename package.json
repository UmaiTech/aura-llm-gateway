{
  "name": "aura-llm-gateway-root",
  "version": "0.0.0",
  "private": true,
  "description": "Root package.json — exists solely to declare deps used by the Vercel serverless functions in /api/. The actual apps live in apps/landing/, apps/chat/, apps/admin/ with their own package.json files.",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "better-auth": "1.6.11",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/pg": "^8.11.10",
    "typescript": "~5.7.3"
  }
}
